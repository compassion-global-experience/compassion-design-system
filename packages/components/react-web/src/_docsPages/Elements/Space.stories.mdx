import { Meta } from '@storybook/addon-docs';
import { space } from '@compassion-gds/elements';
import { styled } from '@storybook/theming';

<Meta title="Elements/Space" />

# Space

Use the `space` element to provide consistency in the positioning of layouts or
components that use the Design System. The object provided by the `space`
element returns a scale of values calculated against a 16px base.

export const Space = styled.div`
  display: grid;
  grid-template-rows: 1fr auto;
  font-family: 'IBM Plex Mono';
  font-size: 0.625rem;
  align-items: center;
  justify-items: center;
  text-align: center;
  &::before {
    content: '';
    display: block;
    height: ${(props) => props.size};
    width: ${(props) => props.size};
    background: ${(props) => props.color};
  }
`;

<section
  style={{
    display: 'flex',
    justifyContent: 'space-around',
    flexWrap: 'wrap',
    marginBottom: '2rem',
  }}
>
  <Space size={space.minus2} color="#38182F46">
    space.minus2
  </Space>
  <Space size={space.minus1} color="#FF00EA">
    space.minus1
  </Space>
  <Space size={space.base} color="#FF00EA21">
    space.base
  </Space>
  <Space size={space.plus1} color="#7A00EE21">
    space.plus1
  </Space>
  <Space size={space.plus2} color="#0029FF21">
    space.plus2
  </Space>
  <Space size={space.plus3} color="#0086BF21">
    space.plus3
  </Space>
  <Space size={space.plus4} color="#14FF0021">
    space.plus4
  </Space>
  <Space size={space.plus5} color="#FFF50021">
    space.plus5
  </Space>
  <Space size={space.plus6} color="#FF6B0021">
    space.plus6
  </Space>
  <Space size={space.plus7} color="#FF000021">
    space.plus7
  </Space>
</section>

## Usage

These object properties can be interpolated in CSS-in-JS or anywhere else that
specific pixel values are needed:

```js
return css`
  .my-component {
    padding: ${space.base} ${space.plus2}; /* Renders `padding: 8px 16px;` */
  }
`;
```

While these concrete values could be stored as variables within an application’s
codebase, leaning on the `space` element allows a single source of truth to flow
through every piece of the Design System, effortlessly connecting the Figma
library with the coded components.

export const Template = () => {};
