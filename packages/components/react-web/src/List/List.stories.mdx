import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';
import { List } from './List';

<Meta
  title="Layouts/List"
  component={List}
  argTypes={{
    ordered: { control: { type: 'boolean' } },
    fullWidth: { control: { type: 'boolean' } },
    stripeColor: { control: { type: 'color' } },
  }}
/>

# List

The List component should be used for continuous vertical groups of related
content. List items may contain text as well as rich content like images, icons,
or other markup. No matter the composition, each item should be appear
consistent with those around it and the items should be ordered logically.

Note that plaintext lists that don’t require additional styling can be rendered
without this component using the semantically appropriate `ul` or `ol` element.

export const Template = (args) => (
  <List {...args}>
    <li>Compassion</li>
    <li>Known</li>
    <li>Loved</li>
    <li>Connected</li>
  </List>
);

export const StripedTemplate = (args) => (
  <List {...args}>
    <li>Compassion</li>
    <li>Known</li>
    <li>Loved</li>
    <li>Connected</li>
  </List>
);

<Canvas>
  <Story name="Default" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

<ArgsTable story="Default" />

## Options

The padding and width of lists may be specified; alternating rows may be shown
with zebra stripes when a `stripeColor` is specified.

<Canvas>
  <Story
    name="Striped"
    args={{ stripeColor: '#f0f0f0', itemPadding: 'medium', fullWidth: true }}
  >
    {StripedTemplate.bind({})}
  </Story>
</Canvas>
