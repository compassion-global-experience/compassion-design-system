import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import { AvatarGroup } from './AvatarGroup';

# AvatarGroup

Use an AvatarGroup to show that many
[Avatars](?path=/docs/layouts-avatars-avatar) are related to each other or are
associated with adjacent content.

<Canvas>
  <Story id="layouts-avatars-avatargroup--default" />
</Canvas>

**Note:** If `size` or `shape` props are furnished, they will override any
`size` or `shape` props specified on children of the AvatarGroup.

<ArgsTable of={AvatarGroup} />
